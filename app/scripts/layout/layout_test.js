'use strict';

describe('myApp.layout module', function() {

  beforeEach(module('myApp.layout'));

  describe('layout controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var layoutCtrl = $controller('layoutCtrl');
      expect(layoutCtrl).toBeDefined();
    }));

  });
});