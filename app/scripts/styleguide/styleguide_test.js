'use strict';

describe('myApp.styleguide module', function() {

  beforeEach(module('myApp.styleguide'));

  describe('styleguide controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var styleguideCtrl = $controller('styleguideCtrl');
      expect(styleguideCtrl).toBeDefined();
    }));

  });
});